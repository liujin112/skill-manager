#!/usr/bin/env python3
# Entry point for the `skill` CLI. Implementation lives in lib/skillcli/.
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent / "lib"))

from skillcli.cli import main  # noqa: E402

if __name__ == "__main__":
    raise SystemExit(main())
